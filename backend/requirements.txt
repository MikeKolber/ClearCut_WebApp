Flask>=3.0,<4.0
flask-cors>=4.0,<6.0
gunicorn>=21.2
bcrypt>=4.0
boto3>=1.34
numpy>=1.24
nptdms>=1.9
# Python 3.12+ doesn't ship pkg_resources by default. pyatmos (and a few
# other older scientific deps) still import it, so we install setuptools
# explicitly to make pkg_resources available.
setuptools>=68
